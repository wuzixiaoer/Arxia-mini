<view class="container">
  <view class="page-body">
    <swiper indicator-dots="{{indicatorDots}}" autoplay="{{autoplay}}" interval="{{interval}}" duration="{{duration}}" bindchange='onSlideChangeEnd'>
  <block wx:for="{{background}}">
    <swiper-item>
      <image src="{{item}}" class="slide-image" style="width: 200px; height: 200px"/>
    </swiper-item>
  </block>
</swiper>
<button bindtap="confirm" > confirm </button>
    </view>
   </view> 

    